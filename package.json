{
  "name": "@karim-w/ts-results",
  "version": "0.1.0",
  "description": "A Result Handler for Typescript Similar to Rust's Result Enum",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && node esbuild.js && tsc"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "Karim-W",
  "repository": {
    "type": "git",
    "url": "https://github.com/Karim-W/ts-results.git"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.18.7",
    "esbuild-node-externals": "^1.8.0",
    "typescript": "^5.1.3"
  }
}
